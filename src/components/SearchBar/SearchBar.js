import React from "react";
import { StyleSheet } from "react-native";
import Input from "../Input/Input";

const SearchBar = () => {
  return <Input placeholder="Search" icon="search" />;
};

const styles = StyleSheet.create({});

export default SearchBar;
